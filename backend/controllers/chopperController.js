const Chopper = require('../models/Chopper'); exports.getChoppers = async (req, res) => { try { const choppers = await Chopper.find(); res.status(200).json(choppers); } catch (error) { res.status(500).json({ error: error.message }); } }; exports.createChopper = async (req, res) => { try { const newChopper = new Chopper(req.body); await newChopper.save(); res.status(201).json(newChopper); } catch (error) { res.status(500).json({ error: error.message }); } }; exports.deleteChopper = async (req, res) => { try { await Chopper.findByIdAndDelete(req.params.id); res.status(200).json({ message: 'Chopper deleted successfully' }); } catch (error) { res.status(500).json({ error: error.message }); } };